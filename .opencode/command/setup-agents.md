---
description: Setup and validate all project agents
agent: build
---
Let's initialize and validate all the agents for the Twind project. Please:

1. Verify the agent definitions in the AGENTS.md file are properly configured
2. Test that the Build agent can access all necessary tools (write, edit, bash, read, glob, grep)
3. Test that the Plan agent has read-only access (read, glob, grep only)
4. Verify the three subagents are properly configured:
   - Template Specialist (Hugo templating expertise)
   - Styles & Layout Specialist (Tailwind CSS and responsive design)
   - Content & SEO Specialist (Markdown content and SEO)
5. Confirm the model is set to OpenRouter Haiku
6. Report any configuration issues or missing pieces

This will ensure all agents are ready to work on Twind Hugo projects.
